/**
 * Legacy wrapper kept for compatibility with earlier docs/scripts.
 *
 * The source of truth for quizzes now lives in context/assessments/*.json.
 * Use seed-assessments.ts directly for new work.
 */

import "./seed-assessments.js";
